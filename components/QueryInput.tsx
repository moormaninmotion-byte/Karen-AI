import React from'react';import{SendIcon}from'./Icons';interface P{value:string;onChange:(v:string)=>void;onSubmit:()=>void;isLoading:boolean}const M=500;const QI:React.FC<P>=({value:v,onChange:c,onSubmit:s,isLoading:l})=>{const h=(e:React.FormEvent)=>{e.preventDefault();if(v.trim()&&!l)s()};const n=v.length;const C=n>=M?'text-red-500':n>M*0.9?'text-yellow-500':'text-gray-500';return(<form onSubmit={h} className="w-full max-w-2xl mx-auto"><div className="relative"><textarea value={v} onChange={(e)=>c(e.target.value)} placeholder="Ask a question..." className="w-full p-4 pr-20 bg-gray-800 border-2 border-gray-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all resize-none shadow-lg" rows={2} disabled={l} maxLength={M} onKeyDown={(e)=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();h(e)}}}/>
<button type="submit" disabled={l||!v.trim()} className="absolute top-1/2 right-4 transform -translate-y-1/2 p-2 rounded-full bg-purple-600 text-white hover:bg-purple-700 disabled:bg-gray-600 disabled:cursor-not-allowed focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-purple-500 transition-colors" aria-label="Submit"><SendIcon/></button></div><p className={`text-right text-xs mt-1 pr-2 ${C}`}>{n}/{M}</p></form>)};export default QI;